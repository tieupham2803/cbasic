#include<stdio.h>
#include<string.h>


void nhap_mang(int a[],int n)
{
int i;
for(i=1;i<=n;i++)
{
printf("\tnhap phan tu vi tri thu %d: ",i);
scanf("%d",&a[i]);
}
} 

void in(int a[])
{
int i;
for(i=1;i<=10;i++)
{

printf(" %d ",a[i]);
}
} 

void duyetmang(int a[],int x, int n){
  int *ptr1,*ptr2;
  // ptr1 = &a[0];
  n =10;
  for (ptr1 = ptr2 = &a[0]; i < n; ++i)
  {
    /* code */
    if (*ptr1 !=x)
    {
      /* code */
      *ptr2 = *ptr2+4;
      *ptr2
    }
  }
}
int main()
{
  int a[50],x,n;

  printf("Nhap vao so nguyen x\n"); 
  scanf("%d",&x);
  printf("Nhap vao so nguyen n la so luong phan tu cua mang a\n"); 
    scanf("%d",&n);

  printf("\nNhAP CAC PHAN TU CUA MANG : \n\n"); 
  nhap_mang(a,n);
// printf("\nMANG DA NHAP : \n");
// in(a); 
  duyetmang(&a[0],x,n);
  printf("%d\n",a );

  printf("%d\n",a[1] );

  printf("%d\n",a[0] );

  printf("%d\n",&a[0] );

  printf("%d\n",&a[1] );
  return 0;
}
